{
  "description": "Nudge agents to use the paperless skill for pngx commands",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/nudge-skill.sh"
          }
        ]
      }
    ]
  }
}
